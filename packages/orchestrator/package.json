{
  "name": "@alan/orchestrator",
  "version": "0.1.0",
  "private": true,
  "main": "src/index.ts",
  "types": "src/index.ts",
  "bin": {
    "alan": "src/bin/alan-cli.ts"
  },
  "scripts": {
    "build": "bun build ./src/index.ts --outdir ./dist --target bun",
    "typecheck": "tsc --noEmit",
    "lint": "prettier --check src/",
    "test": "bun test",
    "start": "bun run src/bin/alan-cli.ts"
  },
  "dependencies": {
    "@alan/shared": "workspace:*",
    "@alan/llm-gateway": "workspace:*",
    "@alan/tool-registry": "workspace:*",
    "@alan/telemetry": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.8"
  }
}
