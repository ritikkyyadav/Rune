{
  "name": "@rune/orchestrator",
  "version": "0.3.0",
  "private": true,
  "main": "src/index.ts",
  "types": "src/index.ts",
  "bin": {
    "rune": "src/bin/rune-cli.ts"
  },
  "scripts": {
    "build": "bun build ./src/index.ts --outdir ./dist --target bun",
    "typecheck": "tsc --noEmit",
    "lint": "prettier --check src/",
    "test": "bun test ../../tests/unit/orchestrator",
    "start": "bun run src/bin/rune-cli.ts"
  },
  "dependencies": {
    "@rune/llm-gateway": "workspace:*",
    "@rune/protocol": "workspace:*",
    "@rune/sdk": "workspace:*",
    "@rune/shared": "workspace:*",
    "@rune/telemetry": "workspace:*",
    "@rune/tool-registry": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.8"
  }
}
