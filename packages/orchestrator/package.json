{
  "name": "@gear/orchestrator",
  "version": "0.3.0",
  "private": true,
  "main": "src/index.ts",
  "types": "src/index.ts",
  "bin": {
    "gear": "src/bin/gear-cli.ts"
  },
  "scripts": {
    "build": "bun build ./src/index.ts --outdir ./dist --target bun",
    "typecheck": "tsc --noEmit",
    "lint": "prettier --check src/",
    "test": "bun test ../../tests/unit/orchestrator",
    "start": "bun run src/bin/gear-cli.ts"
  },
  "dependencies": {
    "@gear/shared": "workspace:*",
    "@gear/llm-gateway": "workspace:*",
    "@gear/tool-registry": "workspace:*",
    "@gear/telemetry": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.8"
  }
}
