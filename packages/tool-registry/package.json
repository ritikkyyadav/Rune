{
  "name": "@alan/tool-registry",
  "version": "0.1.0",
  "private": true,
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "bun build ./src/index.ts --outdir ./dist --target bun",
    "typecheck": "tsc --noEmit",
    "lint": "prettier --check src/",
    "test": "bun test"
  },
  "dependencies": {
    "@alan/shared": "workspace:*",
    "@alan/llm-gateway": "workspace:*",
    "@mozilla/readability": "^0.5.0",
    "linkedom": "^0.18.5",
    "turndown": "^7.2.0"
  },
  "devDependencies": {
    "typescript": "^5.8",
    "@types/turndown": "^5.0.5"
  }
}
