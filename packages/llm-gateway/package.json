{
  "name": "@gear/llm-gateway",
  "version": "0.3.0",
  "private": true,
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "bun build ./src/index.ts --outdir ./dist --target bun",
    "typecheck": "tsc --noEmit",
    "lint": "prettier --check src/",
    "test": "bun test"
  },
  "dependencies": {
    "@gear/shared": "workspace:*",
    "@gear/telemetry": "workspace:*",
    "@anthropic-ai/sdk": "^0.39",
    "openai": "^4"
  },
  "devDependencies": {
    "typescript": "^5.8"
  }
}
