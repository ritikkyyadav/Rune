# The .vsix should be the extension, not the repository. Everything here is
# either bundled into out/extension.js already or has no business shipping.
src/**
test/**
out/test/**
esbuild.mjs
tsconfig.json
node_modules/**
.turbo/**
.gitignore
**/*.map
**/*.vsix
