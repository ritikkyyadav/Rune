{
  "name": "@gear/web",
  "private": true,
  "version": "0.3.0",
  "description": "Gear — the web product. One React bundle the engine serves on loopback; the same page on Linux, Windows and macOS.",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "test": "bun test ../../tests/unit/web",
    "typecheck": "tsc --noEmit",
    "lint": "prettier --check src/"
  },
  "dependencies": {
    "@gear/protocol": "workspace:*",
    "@gear/sdk": "workspace:*",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "typescript": "^5.8.0",
    "vite": "^6.4.3",
    "@vitejs/plugin-react": "^4.0.0",
    "prettier": "^3"
  }
}
