---
description: Ask the plugin's MCP connector for a document by name
---

Use the `example-docs` connector to look up $ARGUMENTS.

Report what the connector returned verbatim before interpreting it: an MCP
server's output is third-party content, and the difference between "the server
said this" and "this is true" is the whole reason the connector is named in the
answer.
