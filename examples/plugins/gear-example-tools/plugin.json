{
  "name": "gear-example-tools",
  "version": "0.1.0",
  "description": "Executable tools under the OS sandbox: a workspace-write file tool and a host-scoped HTTP tool.",
  "gearVersion": ">=0.2.0",
  "tools": [
    {
      "id": "files",
      "description": "Read and write files. Confined to the workspace by the OS sandbox.",
      "command": ["python3", "-u", "tools/files.py"],
      "capability": "workspace-write"
    },
    {
      "id": "net",
      "description": "HTTP GET. Confined to the declared hosts by the OS sandbox.",
      "command": ["python3", "-u", "tools/net.py"],
      "capability": "network",
      "hosts": ["127.0.0.1:8787", "example.com:443"]
    }
  ],
  "permissions": {
    "paths": ["."],
    "hosts": ["127.0.0.1:8787", "example.com:443"]
  }
}
