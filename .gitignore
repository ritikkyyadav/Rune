# Rust
/target
**/target/
**/*.rs.bk

# Node / Bun
node_modules/
.turbo/
dist/
bun.lockb

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Alan runtime
.alan/cache/
.alan/logs/
# Local code indexes (rebuilt on demand; WAL/SHM siblings included)
.alan/symbols.db*
.alan/search.db*
# Detached-run worktrees (git worktree checkouts; branches carry the work)
.alan/worktrees/
# MCP server config — local, may contain credentials in headers (see mcp.json.example)
.alan/mcp.json

# Environment
.env
.env.local

# Build artifacts
*.dmg
*.deb
*.rpm
*.AppImage

# Test artifacts
tests/fixtures/tmp/

# Playwright MCP session snapshots (browser tool scratch)
.playwright-mcp/
