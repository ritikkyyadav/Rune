{
  "name": "@rune/eval",
  "version": "0.4.1",
  "private": true,
  "main": "runner.ts",
  "scripts": {
    "eval": "bun run runner.ts"
  },
  "dependencies": {
    "@rune/orchestrator": "workspace:*",
    "@rune/llm-gateway": "workspace:*",
    "@rune/shared": "workspace:*",
    "@rune/telemetry": "workspace:*",
    "@rune/tool-registry": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.8"
  }
}
